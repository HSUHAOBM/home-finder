# 一鍵開啟找房介面

最簡單的方式：直接雙擊專案資料夾裡的 `開啟找房介面.cmd`。

它會自動打開瀏覽器。先選擇大樓／公寓／華廈、透天／車墅或預售屋，再選擇「每日更新」或「完整盤點」並開始搜尋。搜尋期間請保持黑色命令視窗開啟，完成後頁面會自動更新。

也可以在 PowerShell 執行：

```powershell
uv run python -m home_finder.web_app_v7
```

如果瀏覽器沒有自動開啟，請前往：

```text
http://127.0.0.1:8765
```

關閉命令視窗就會停止介面。如果在搜尋中關閉，這一輪不會完成；上一輪成功的結果仍會保存在 `output/current-results.json`，下次開啟時會直接顯示。

頁面可直接調整行政區、價格、坪數、房數、車位、屋齡與偏好。設定會存入本機的 `config.user.json`，不會納入 Git。
