# 一鍵開啟找房介面

最簡單的方式：直接雙擊專案資料夾裡的 `開啟找房介面.cmd`。

它會打開黑色 CMD 視窗並自動開啟瀏覽器。CMD 視窗保持開啟就代表服務正在執行。程式或網頁檔案修改後，服務會自動重載；切回瀏覽器分頁時，頁面會偵測新服務並自動刷新，不必再按 `Ctrl+C` 重開。先選擇大樓／公寓／華廈、透天／車墅或預售屋，再選擇「每日更新」或「完整盤點」並開始搜尋。搜尋期間請保持 CMD 視窗開啟，完成後頁面會自動更新。

每張房源卡都可加入收藏；「我的收藏」會集中顯示過去收藏，並保留收藏時間與最近看到時間。房源暫時未出現在目前結果時，收藏快照仍會保留。

頁面頂端會顯示最近一次成功爬蟲的日期、時間、購屋目標、搜尋模式與讀取筆數；切換購屋目標後，摘要區也會顯示該目標自己的最近成功時間。只有成功完成的搜尋才會更新，失敗或中途停止會保留上一筆成功紀錄。

也可以在 PowerShell 執行：

```powershell
uv run python -m home_finder.web_app_v8
```

如果瀏覽器沒有自動開啟，請前往：

```text
http://127.0.0.1:8765
```

要停止服務，請在 CMD 視窗按 `Ctrl+C`，或直接關閉視窗。若畫面詢問是否終止批次工作，輸入 `Y` 後按 Enter。如果在搜尋中停止服務，這一輪不會完成；上一輪成功的結果仍會保存在 `output/current-results.json`，下次開啟時會直接顯示。

頁面可直接調整行政區、價格、坪數、房數、車位、屋齡與偏好。設定會存入本機的 `config.user.json`，不會納入 Git。

每次按「儲存並套用」時，系統會把修改前與修改後的完整條件保存在本機 `data/settings_history.json`。下次可從「調整條件 → 沿用過去條件」選擇版本、載入表單檢查後再次套用；連續儲存相同內容不會產生重複版本。
